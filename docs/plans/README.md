# Plans

Design docs and rollout plans for work that hasn't fully landed. Once a plan is fully
implemented, move it to [`completed/`](completed/) rather than deleting it — the reasoning
behind a decision (why a route is structured a certain way, why a feature was cut) is often
worth more after the fact than the checklist itself.

## Active

| Doc | Status |
|-----|--------|
| [error-reporting.md](error-reporting.md) | Partially shipped — see status note at top of the doc |

## Completed

[`completed/`](completed/) holds the **Pre-Release Refinement Initiative** (5 phases + the UX
audit that motivated them, July 2026 — route groups, landing page, Home redesign, flow wiring,
polish) and the **shared frontend utilities extraction**. Both are fully implemented and
verified against the current codebase. Read them for historical "why," not as a task list.
